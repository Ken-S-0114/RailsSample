# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RailsSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RailsSample
  pod 'Alamofire'

  target 'RailsSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RailsSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
